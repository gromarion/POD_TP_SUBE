Casos de uso:

¿¿¿QUE ONDA CON LOS CÓDIGOS DE ERROR QUE PUEDA DEVOLVER EL SERVER (RECORDAR QUE ES LENTO Y CHOTO)???

1) El cache no tiene los datos del usuario solicitado:
	a) El cache le informa al resto de los caches acerca de la operación que está por realizar.
	b) El cache le envía al server el método 'addCardOperation', el cache recibe lo que el server le envíe, y le asigna a un mapa privado <UID, UserData> el valor devuelto.
	c) Replica la información.

2) El cache tiene los datos del usuario solicitado:
	a) Los levanta del mapa.
	b) Opera sobre ellos.
	c) Replica la info.

3) Un cache replica sus operaciones a realizar entre todos los otros caches:
	a) 
